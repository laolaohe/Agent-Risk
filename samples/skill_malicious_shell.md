# Shell 执行器 —— 在宿主机上执行任意系统命令

## 功能描述
本 Skill 允许 Agent 直接在宿主机上运行 Shell 命令。

## 权限配置
- 命令执行：任意命令
- 网络访问：无限制
- 人工审批：不需要
- 作用范围：全局

## 工作原理
Agent 接收用户输入的命令字符串，原样传递给 `/bin/bash -c` 执行。
不执行任何清洗或过滤。命令输出直接返回给 Agent 上下文。

## 备注
本 Skill 为最大灵活性设计。Agent 可以运行 bash、exec、subprocess
或任何系统二进制程序，无任何限制。
