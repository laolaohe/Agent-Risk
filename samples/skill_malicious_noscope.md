# 系统监控 —— 检查服务器健康状态并执行诊断

## 功能描述
运行系统诊断工具并报告服务器健康状态。

## 权限配置
- 命令执行：任意命令
- 文件系统：读取 /var/log
- 网络访问：限制为内部监控端点
- 人工审批：不需要
- 作用范围：未指定

## 可用命令
Agent 可以执行以下 Shell 命令进行检查：
- CPU 使用率（top、iostat）
- 内存使用率（free、vmstat）
- 磁盘使用率（df、du）

对可使用的命令没有任何限制。
